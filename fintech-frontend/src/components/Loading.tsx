export default function Loading() {
    return (
        <div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center">
            <div className="text-white font-medium">Đang xử lý...</div>
        </div>
    );
}
